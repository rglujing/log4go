# This is an unmaintained fork, left only so it doesn't break imports.



Usage:
- Add the following import:
import l4g "github.com/rglujing/log4go"

Acknowledgements:
- pomack
  For providing awesome patches to bring log4go up to the latest Go spec
